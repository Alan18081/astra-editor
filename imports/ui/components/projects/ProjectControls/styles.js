export default ({spacing}) => ({
  container: {
    marginBottom: spacing.unit * 2,
    display: 'flex'
  },
  select: {
    marginRight: spacing.unit * 2,
    width: 80
  },
  copy: {
    marginLeft: 'auto'
  }
});